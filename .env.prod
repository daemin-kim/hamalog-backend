# Production Environment Configuration for Hamalog
# This file contains environment variables for production deployment
# Usage: source .env.prod before running deploy.sh

# Database Configuration
DB_NAME=Hamalog
DB_USERNAME=hamalog_user
DB_PASSWORD=H@m@l0g_Pr0d_DB_P@ssw0rd_2025!
MYSQL_ROOT_PASSWORD=MySql_R00t_Str0ng_P@ssw0rd_2025!
SPRING_DATASOURCE_USERNAME=hamalog_user
SPRING_DATASOURCE_PASSWORD=H@m@l0g_Pr0d_DB_P@ssw0rd_2025!

# JWT Configuration - Retrieved exclusively from Vault in production
# JWT_SECRET= # Retrieved from Vault: secret/hamalog:jwt-secret
JWT_EXPIRY=3600000

# Kakao OAuth2 Configuration - Retrieved exclusively from Vault in production
# KAKAO_CLIENT_ID= # Retrieved from Vault: secret/hamalog:kakao-client-id
# KAKAO_CLIENT_SECRET= # Retrieved from Vault: secret/hamalog:kakao-client-secret

# Optional: Kakao Redirect URI (if different from default)
# KAKAO_REDIRECT_URI=https://your-domain.com/api/auth/kakao/callback

# Spring Profile
SPRING_PROFILES_ACTIVE=prod

# Frontend URL (update with actual domain)
# FRONTEND_URL=https://your-domain.com

# CORS Origins (update with actual domain)
# ALLOWED_ORIGINS=https://your-domain.com

# File Upload Directory
# UPLOAD_DIR=/data/hamalog/images

# Log Directory
# LOG_DIR=/var/log/hamalog

# Vault Configuration for secure secret management
HAMALOG_VAULT_ENABLED=true
HAMALOG_VAULT_URI=http://vault:8200
HAMALOG_VAULT_TOKEN=hamalog-dev-token
HAMALOG_VAULT_KV_BACKEND=secret
HAMALOG_VAULT_KV_DEFAULT_CONTEXT=hamalog

# Data Encryption Key - Retrieved exclusively from Vault in production
# HAMALOG_ENCRYPTION_KEY= # Retrieved from Vault: secret/hamalog:encryption-key