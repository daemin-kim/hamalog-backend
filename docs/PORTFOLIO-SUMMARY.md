# Hamalog 포트폴리오 요약

> **헬스케어 복약 관리 백엔드** | Java 21 · Spring Boot 3.4.5 · JPA · Redis · MySQL

---

## 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | Hamalog (하마로그) |
| **설명** | 복약 스케줄, 마음 일기, 부작용 추적을 통합 관리하는 헬스케어 백엔드 |
| **기간** | 2025.08 ~ 현재 (약 5개월) |
| **역할** | 백엔드 전담 개발 (설계, 구현, 테스트, 배포 100%) |
| **API** | 50+ 엔드포인트 |
| **테스트** | 1,300+ 테스트 케이스, 80%+ 커버리지 |

---

## 기술 스택

**Backend**: Java 21, Spring Boot 3.4.5, Spring Security, Spring Data JPA  
**Database**: MySQL 8.0, Redis 7, Flyway  
**Security**: JWT + CSRF 이중 보호, AES-256-GCM 암호화  
**DevOps**: Docker, Cloudflare Tunnel (Zero Trust), Nginx  
**Testing**: JUnit 5, Mockito, ArchUnit, JaCoCo  

---

## 핵심 기능

| 도메인 | 주요 기능 |
|--------|----------|
| **인증/보안** | JWT 인증, CSRF 방어, Refresh Token Rotation, Rate Limiting, OAuth2(카카오) |
| **복약 관리** | 스케줄 CRUD, 이미지 업로드, 복약 기록, 이행률 통계, 그룹 관리 |
| **마음 일기** | 템플릿/자유 형식, 기분 통계, 캘린더 뷰, 하루 1회 제한 |
| **부작용** | 다중 부작용 기록, 정도 평가, Redis 캐싱 |
| **알림** | Redis Stream 메시지 큐, FCM 푸시, DLQ + Discord 알림 |

---

## 기술적 도전과 해결

### 1️⃣ JWT + CSRF 이중 보호
- **문제**: JWT만으로는 CSRF 공격에 취약
- **해결**: Redis 기반 CSRF 토큰 + Refresh Token Rotation
- **결과**: CSRF 완전 차단, Stateless 아키텍처 유지

### 2️⃣ AOP 기반 리소스 소유권 검증
- **문제**: 모든 API에서 소유권 검증 코드 중복 (200줄+)
- **해결**: `@RequireResourceOwnership` 커스텀 어노테이션
- **결과**: 1줄로 선언적 검증, 관심사 분리

### 3️⃣ JPA N+1 문제 해결
- **문제**: 10개 조회 시 21개 쿼리 발생
- **해결**: @EntityGraph + DTO Projection + 배치 조회
- **결과**: 쿼리 95% 감소, 응답 시간 70% 단축

### 4️⃣ 민감 정보 암호화
- **문제**: 개인정보보호법 암호화 요건
- **해결**: AES-256-GCM 필드 단위 암호화
- **결과**: 법적 요건 충족, DB 탈취 시에도 복호화 불가

### 5️⃣ 비동기 알림 처리
- **문제**: FCM 동기 호출로 API 응답 200~500ms 지연
- **해결**: Redis Stream 메시지 큐 + DLQ
- **결과**: 알림 발송 시간 API에서 제거, 실패 추적 가능

---

## 아키텍처 특징

```
┌──────────────────────────────────────────────────────────┐
│ Controller - @RequireResourceOwnership, @Valid          │
├──────────────────────────────────────────────────────────┤
│ Service - @Transactional, @Cacheable, @Retryable        │
├──────────────────────────────────────────────────────────┤
│ Domain - JPA Entity, @Version(낙관적 락), Domain Event  │
├──────────────────────────────────────────────────────────┤
│ Repository - Spring Data JPA, Redis                     │
└──────────────────────────────────────────────────────────┘
```

- **Zero Trust 배포**: Cloudflare Tunnel → 외부 포트 노출 없음
- **AOP 횡단 관심사**: 소유권 검증, 로깅, 성능 측정, 재시도
- **로그 분리**: application.log, security.log, audit.log, performance.log

---

## 문서화

| 문서 | 개수 | 목적 |
|------|------|------|
| ADR | 7개 | 아키텍처 결정 근거 |
| 패턴 가이드 | 6개 | 보안, 캐싱, JPA, 에러처리 등 |
| API 명세서 | 900줄 | 프론트엔드 연동 |
| 코딩 컨벤션 | 400줄 | 코드 스타일 통일 |

---

## 배운 점

| 영역 | 핵심 인사이트 |
|------|--------------|
| **보안** | JWT + CSRF 이중 보호, Refresh Token Rotation의 실무 적용 |
| **성능** | N+1 해결, 캐싱 전략, 비동기 처리로 응답 시간 최적화 |
| **설계** | AOP 선언적 프로그래밍으로 관심사 분리 |
| **테스트** | ArchUnit으로 아키텍처 규칙 강제, 테스트 피라미드 실천 |

---

> 📄 상세 포트폴리오: [PORTFOLIO.md](./PORTFOLIO.md)  
> 📧 GitHub: [github.com/daemin-kim](https://github.com/daemin-kim)

